#include <bits/stdc++.h>
using namespace std;

int query(int l, int r) {
    cout << "xor " << l << " " << r << endl;
    cout.flush();
    
    int response;
    cin >> response;
    if (response == -1) exit(0);
    return response;
}

void reportAnswer(int a, int b, int c) {
    cout << "ans " << a << " " << b << " " << c << endl;  
    cout.flush(); 
}

int main() {
    int t;
    cin >> t;
    while (t--) {
        int n;
        cin >> n; 
    }
    
    return 0;
}
